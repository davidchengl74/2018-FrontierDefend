#import "CGame.h"

//@hao.ly
extern "C" void getUniqueIDForDevice(char * uid)
{
    //sprintf(uid, "%s", [[[[ASIdentifierManager sharedManager] advertisingIdentifier] UUIDString] UTF8String]);
    sprintf(uid, "%s","waiting");
}